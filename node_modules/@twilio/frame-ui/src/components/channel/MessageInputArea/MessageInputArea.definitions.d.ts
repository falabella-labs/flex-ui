/// <reference types="react" />
import { InputAreaStyleProps } from "../MessageInput/MessageInput.definition";
import { MessageState } from "../../../state/ChatChannelInputState";
import { AriaProps } from "../../AriaProps";
import * as ChannelState from "../../../state/ChannelState";
import { KeyboardShortcuts } from "../../../Markdown/MarkdownHelper.definitions";
export interface CharLimitStyleProps extends InputAreaStyleProps {
    inputTextLength: number;
    charLimit: number;
}
export declare type KeyboardShortcutsHandlers = {
    [k: string]: (event?: any) => void;
};
export declare type OnShortCutPressedFunction = (shortcutId: string, event?: React.ChangeEvent<HTMLTextAreaElement>) => void;
export interface InputAreaKeyboardShortcuts {
    keys: KeyboardShortcuts;
    onShortCutPressed?: OnShortCutPressedFunction;
}
export interface MessageInputAreaProps extends Partial<InputAreaStyleProps> {
    channelSid: string;
    channel: ChannelState.ChannelState;
    placeholder?: string;
    onSendMessage: () => void;
    disabled?: boolean;
    returnKeySendsMessage: boolean;
    charLimit: number;
    inputTextLength: number;
    selectionStart?: number;
    onChange?: (state: MessageState) => void;
    messageState: MessageState;
    textAreaAriaProps?: AriaProps;
    keyboardShortcuts?: InputAreaKeyboardShortcuts;
}
