import { CountryData } from "../../../CountryManager/CountryManager.definition";
import { CSSProps } from "../../CSSProps";
export interface CountrySelectorProps {
    defaultCountry?: string;
    selectedCountry?: string;
    onCountryChange: (country: CountryData) => void;
}
export interface CountrySelectorState {
    allCountries: Array<CountryData>;
    filteredCountries: Array<CountryData>;
    flagPositions: {
        [key: string]: number;
    };
}
export interface CountrySelectorThemeProps {
    CountryList: CSSProps;
    CountryItem: CSSProps & {
        Selected: CSSProps;
    };
    Flag: CSSProps;
}
