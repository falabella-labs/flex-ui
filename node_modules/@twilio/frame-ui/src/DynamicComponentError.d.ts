/**
 * DynamicComponentError
 * Used when an error is thrown inside a custom component
 *
 * @private
 * @extends Error
 */
export declare class DynamicComponentError extends Error {
    key: string;
    constructor(componentName: string);
}
